{
    "name": "base-delta-immediate-compression",
    "version": "1.0.0",
    "scripts": {
        "deploy:gh-pages": "rm -rf ./dist/ && parcel build --public-url ./ ./index.html && gh-pages -d dist",
        "build": "tsc --build",
        "watch": "tsc --build --watch"
    },
    "main": "dist/index.js",
    "license": "MIT",
    "devDependencies": {
        "@nimashoghi/eslint-config": "^0.0.7",
        "@types/jest": "^25.2.1",
        "@types/node": "^13.13.4",
        "eslint": "^6.8.0",
        "gh-pages": "^2.2.0",
        "jest": "^25.4.0",
        "parcel-bundler": "^1.12.4",
        "prettier": "^2.0.5",
        "ts-jest": "^25.4.0",
        "ts-node": "^8.9.1",
        "typescript": "^3.8.3"
    }
}
